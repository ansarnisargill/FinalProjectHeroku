

<nav class="navbar w3-card-8 navbar-expand-md navbar-dark bg-primary text-white">
    <a class="navbar-brand" href="#">BetterBrain</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-item nav-link active" href="#">Home <span class="sr-only">(current)</span></a>
                </li>
            <li class="nav-item ">
                <a class="nav-item nav-link" href="#">Features</a>
                </li>
            <li class="nav-item ">
                <a class="nav-item nav-link" href="#">Pricing</a>
                </li>
                @{
                    FinalProject.Context.FinalContext db = new FinalProject.Context.FinalContext();
                    if (Session["User"] != null)
                    {
                         <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Dropdown link
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">

                         @{  
                             FinalProject.Models.User U1 = (FinalProject.Models.User)Session["User"];
                             var notifications = db.Notifications.Where(c => c.UserID == U1.ID && c.Type == "User").ToList();
                             foreach (var item in notifications)
                             {

                            <a class="dropdown-item" href="@item.Link">@item.Text</a>

                                 }
                             }
                        </div>
                    </li>
                   
                                



                }


            }


        </ul>

    </div>
</nav>

